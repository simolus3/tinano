name: tinano_generator
description: Code generation for the tinano library. To be included as a dev-dependency.

environment:
  sdk: '>=2.0.0 <3.0.0'

dependencies:
  source_gen:
  collection:
  meta:
  code_builder:
  build: ^1.0.0
  build_config:

dev_dependencies:
  test: ^1.0.0
